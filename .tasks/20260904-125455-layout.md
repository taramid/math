# Task for AI Agent

## Context
take into account AGENTS.md

## To-Do
make the simplest modern layout using Tailwind.
responsive.
slim sticky header and footer.
the header has a logo and two buttons.



## Note
you're welcome to make small, atomic commits for each logical change with clean messages
make as least changes as possible


## Interaction Log

Done. The layout is in place and verified end-to-end.

Changes (4 atomic commits on ai/layout):
1. Add slim sticky header/footer layout to base template — templates/base.html.twig:
    - Header: h-12 sticky, translucent blur, logo (badge + wordmark, wordmark hidden on mobile) and two buttons (ghost "About" + primary "Sign in"), responsive within a max-w-5xl container
    - Footer: matching slim h-12 bar with copyright + tagline
    - Body: flex min-h-screen flex-col bg-slate-50 so the footer sticks to the bottom and <main class="flex-1"> holds page content ({% block body %})
2. Remove placeholder styles from app.css — dropped the skyblue body hack; app.css is now just @import "tailwindcss";
   4. CSS link handling — verified the symfonycasts/tailwind bundle (v4.3.3) + asset mapper already injects the compiled stylesheet via importmap('app'), so no manual <link> is needed

Verified: tailwind:build compiles cleanly, rendered the base template through a throwaway controller (removed afterwards), and confirmed the served CSS is the compiled 12 KB Tailwind v4
bundle with no duplicate links. Working tree is clean.

No routes exist yet, so the Symfony welcome page still shows at / — the layout will apply to the first real controller that renders base.html.twig.
